# Frontend 컨테이너: Vue SPA 정적 서빙 전용 (API 라우팅은 앞단 lab nginx가 담당)
server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
